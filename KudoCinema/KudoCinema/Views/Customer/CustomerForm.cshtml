@model KudoCinema.ViewModels.CustomerFormViewModel
@{
    ViewBag.Title = "CustomerForm";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>@ViewBag.FormTitle</h2>
@using (Html.BeginForm("Save", "Customer", FormMethod.Post))
{
    <div class="form-group">
        @Html.LabelFor(m => m.Customer.Name)
        @Html.TextBoxFor(m => m.Customer.Name, new { @class = "form-control" })
    </div>
    <div class="form-group">
        @Html.LabelFor(m => m.Customer.Birthday)
        @Html.TextBoxFor(m => m.Customer.Birthday, "{0: dd/MM/yyyy}", new { @class = "form-control"})
    </div>
    <div class="form-group">
        @Html.LabelFor(m=>m.Customer.MembershipTypeId)
        @Html.DropDownListFor(m => m.Customer.MembershipTypeId, new SelectList(Model.MembershipTypes, "Id", "Name", "Selected value"), new { @class = "form-control"})
    </div>
    <div class="checkbox">
        @Html.CheckBoxFor(m => m.Customer.IsSubcribed) Is Subcribed To Newsletter?
    </div>
    @Html.HiddenFor(m => m.Customer.Id)
    <button class="btn btn-primary">Save</button>

}

